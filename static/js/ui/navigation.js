/**
 * Navigation UI management.
 */
// Placeholder - will be migrated from script.js
export function updateStepIndicators() {
    if (typeof window.updateStepIndicators === 'function') {
        window.updateStepIndicators();
    }
}

export function updateProgress() {
    if (typeof window.updateProgress === 'function') {
        window.updateProgress();
    }
}

export function updateNavigationButtons() {
    if (typeof window.updateNavigationButtons === 'function') {
        window.updateNavigationButtons();
    }
}

export function updateStep2Access() {
    if (typeof window.updateStep2Access === 'function') {
        window.updateStep2Access();
    }
}


